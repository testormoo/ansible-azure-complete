- hosts: localhost
  tasks:
    - name: OperationList
      azure_rm_resource_facts:
        api_version: '2018-06-01-preview'
        # url: /providers/Microsoft.DBforMariaDB/operations
        provider: DBforMariaDB
        resource_type: operations
