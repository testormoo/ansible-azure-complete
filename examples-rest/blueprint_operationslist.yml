- hosts: localhost
  tasks:
    - name: OperationsList
      azure_rm_resource_facts:
        api_version: '2017-11-11-preview'
        # url: /providers/Microsoft.Blueprint/operations
        provider: Blueprint
        resource_type: operations
