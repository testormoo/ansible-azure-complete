- hosts: localhost
  tasks:
    - name: PriceSheet
      azure_rm_resource_facts:
        api_version: '2018-10-01'
        # url: /subscriptions/{subscriptionId}/providers/Microsoft.Consumption/pricesheets/default
        provider: Consumption
        resource_type: pricesheets
        resource_type: default
