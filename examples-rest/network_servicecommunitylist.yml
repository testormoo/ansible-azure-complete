- hosts: localhost
  tasks:
    - name: ServiceCommunityList
      azure_rm_resource_facts:
        api_version: ''
        # url: /subscriptions/{subscriptionId}/providers/Microsoft.Network/bgpServiceCommunities
        provider: Network
        resource_type: bgpServiceCommunities
